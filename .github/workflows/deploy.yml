name: Build and Deploy to ClawCloud

on:
  push:
    branches: [ "main" ]

env:
  IMAGE_NAME: homework-reminder

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: extract-version
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          echo "tag=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "owner=$OWNER" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ steps.extract-version.outputs.owner }}/${{ env.IMAGE_NAME }}:${{ steps.extract-version.outputs.tag }}
            ghcr.io/${{ steps.extract-version.outputs.owner }}/${{ env.IMAGE_NAME }}:latest

      - name: Output image info
        run: |
          echo "Image: ghcr.io/${{ steps.extract-version.outputs.owner }}/${{ env.IMAGE_NAME }}:${{ steps.extract-version.outputs.tag }}"
